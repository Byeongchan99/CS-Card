---
title: 게임 수학·물리
tags: [게임 수학·물리, 요약]
date: 2026-07-30
---

분야 핵심 개념을 얕고 넓게 정리한 노트. 쓱 훑어보며 복습한다.

## 벡터 정규화

정규화는 크기를 1로 맞추는 것. 안 하면 대각선 (1,1)은 크기 √2 ≈ 1.41이라 상하좌우(크기 1)보다 1.41배 빨라짐. 정규화하면 (0.707, 0.707)로 크기 1이 되어 모든 방향이 같은 속도.

## 내적(dot product)

정규화된 두 벡터의 내적은 1이면 같은 방향, 0이면 수직, −1이면 반대. 시야 판정은 적 방향을 정규화해 전방 벡터와 내적한 뒤 `dot >= cos(반각)`이면 시야 안.

<svg viewBox="0 0 300 170" width="300" style="max-width:100%;height:auto" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="두 벡터 사이 각도와 내적"><defs><marker id="dp-b" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0L6,3L0,6Z" fill="#4f83e0"/></marker><marker id="dp-c" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0L6,3L0,6Z" fill="#e2574c"/></marker></defs><line x1="45" y1="130" x2="235" y2="130" stroke="#4f83e0" stroke-width="2.5" marker-end="url(#dp-b)"/><line x1="45" y1="130" x2="175" y2="48" stroke="#e2574c" stroke-width="2.5" marker-end="url(#dp-c)"/><path d="M95 130 A50 50 0 0 0 80 101" fill="none" stroke="currentColor" stroke-width="1.4" opacity="0.55"/><text x="102" y="119" font-size="13" fill="currentColor" opacity="0.7">θ</text><text x="240" y="134" font-size="13" fill="#4f83e0">A</text><text x="180" y="45" font-size="13" fill="#e2574c">B</text><text x="45" y="158" font-size="12" fill="currentColor" opacity="0.7">정규화 후 A·B = cos θ  (1 같은방향 · 0 수직 · −1 반대)</text></svg>

## 외적(cross product)

결과는 두 벡터에 수직인 벡터(법선)이고, 크기는 평행사변형 넓이. 부호로 좌우 판별이 가능해서, 정면 벡터와 적 방향 벡터의 외적 부호를 봄. 2D는 외적이 스칼라(`ax·by − ay·bx`)라 부호가 곧 회전 방향이고, 3D는 외적 벡터의 y성분(또는 기준 축과의 정렬) 부호. 부호↔좌우 대응은 연산 순서·좌표계에 의존하므로(순서를 바꾸면 부호 반전), 유니티에서 `Vector3.Cross(forward, toTarget).y`로 판정하되 부호 방향은 프로젝트에서 한 번 검증한다.

## 부호 있는 각도(signed angle)

내적은 cos(0~180°, 부호 없음)만 줘 방향을 모름. 외적이 방향(부호)을 줌. `signedAngle = Atan2(cross, dot)`(cross = sin 성분/방향, dot = cos 성분). 2D는 외적 스칼라가 바로 부호, 3D는 기준 축(axis)과의 내적 부호 → `Vector3.SignedAngle(from, to, axis)`.

## 벡터 투영과 벽 슬라이딩

벽 법선 n(정규화)에 대해 미끄러질 속도 = `v − (v·n)·n`. `v·n`이 v 중 법선 방향 성분의 크기(스칼라), `(v·n)·n`이 벽 수직 성분 벡터 → 빼면 벽과 평행한 성분만 남음.

<svg viewBox="0 0 300 175" width="300" style="max-width:100%;height:auto" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="속도를 벽 법선 성분과 평행 성분으로 분해"><defs><marker id="ws-v" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0L6,3L0,6Z" fill="#4f83e0"/></marker><marker id="ws-n" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0L6,3L0,6Z" fill="#e2574c"/></marker><marker id="ws-s" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0L6,3L0,6Z" fill="#3fae7a"/></marker></defs><line x1="192" y1="30" x2="192" y2="155" stroke="currentColor" stroke-width="3" opacity="0.3"/><text x="198" y="44" font-size="12" fill="currentColor" opacity="0.6">벽</text><line x1="80" y1="55" x2="188" y2="110" stroke="#4f83e0" stroke-width="2.5" marker-end="url(#ws-v)"/><text x="118" y="72" font-size="13" fill="#4f83e0">v</text><line x1="192" y1="110" x2="150" y2="110" stroke="#e2574c" stroke-width="2" marker-end="url(#ws-n)"/><text x="150" y="103" font-size="13" fill="#e2574c">n</text><line x1="192" y1="112" x2="192" y2="150" stroke="#3fae7a" stroke-width="2.5" marker-end="url(#ws-s)"/><text x="199" y="140" font-size="12" fill="#3fae7a">미끄러짐</text><text x="20" y="170" font-size="12" fill="currentColor" opacity="0.7">slide = v − (v·n) n  (법선 성분 제거)</text></svg>

## 경사면 중력 분해

미끄러지는 힘 = `g − (g·n)·n`. 벽 슬라이딩과 정확히 같은 구조로, 수직 성분을 빼면 경사면 평행 성분만 남음. `(g·n)`은 스칼라라 n을 다시 곱해 벡터로 만드는 것 — 끝의 `·n`이 빠지면 안 됨.

## 반사 벡터(reflection)

입사 d, 정규화된 법선 n일 때 `r = d − 2·(d·n)·n`. 내적 `d·n`이 법선 방향 성분의 크기를 뽑아내고, 그걸 두 배로 빼서 법선 성분만 반전(벽과 평행한 성분은 유지). 각도를 구해 다시 만드는 게 아니라 벡터 공식으로 바로 계산한다. 유니티는 `Vector3.Reflect`.

<svg viewBox="0 0 300 175" width="300" style="max-width:100%;height:auto" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="입사 벡터의 반사"><defs><marker id="rf-d" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0L6,3L0,6Z" fill="#4f83e0"/></marker><marker id="rf-r" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0L6,3L0,6Z" fill="#3fae7a"/></marker><marker id="rf-n" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0L6,3L0,6Z" fill="#e2574c"/></marker></defs><line x1="30" y1="128" x2="270" y2="128" stroke="currentColor" stroke-width="2.5" opacity="0.3"/><line x1="65" y1="50" x2="148" y2="125" stroke="#4f83e0" stroke-width="2.5" marker-end="url(#rf-d)"/><text x="76" y="60" font-size="13" fill="#4f83e0">d</text><line x1="150" y1="128" x2="150" y2="62" stroke="#e2574c" stroke-width="1.8" stroke-dasharray="4 3" marker-end="url(#rf-n)"/><text x="156" y="70" font-size="13" fill="#e2574c">n</text><line x1="152" y1="125" x2="235" y2="50" stroke="#3fae7a" stroke-width="2.5" marker-end="url(#rf-r)"/><text x="222" y="60" font-size="13" fill="#3fae7a">r</text><text x="30" y="160" font-size="12" fill="currentColor" opacity="0.7">r = d − 2 (d·n) n  (법선 성분만 반전)</text></svg>

## 짐벌 락과 쿼터니언

오일러 각은 세 축 회전을 순차 적용하는데, 가운데 축이 90°가 되면 첫 축과 셋째 축의 회전축이 겹쳐 자유도 하나를 잃음(3 → 2). 쿼터니언은 (w, x, y, z) 4개 값으로 축-각도를 한 번에 표현해 순차 누적이 없어 짐벌 락이 안 생기고, slerp로 부드럽게 보간되며 합성이 안정적. 쿼터니언은 행렬이 아니며, 비교 대상은 행렬이 아니라 오일러 각이다.

## Lerp vs Slerp

Lerp는 직선 보간이라 위치에 적합. Slerp는 원호를 따라 일정한 각속도로 보간해 회전·방향에 적합. 회전을 Lerp하면 각속도가 불균일하고 재정규화가 필요해 부자연스러움.

## Quaternion.Slerp

`Slerp(현재, 목표, speed·dt)`로 매 프레임 조금씩 보간. 구면 선형 보간이라 두 회전 사이를 일정 각속도·최단 호로 부드럽게. 오일러 각을 각각 선형 보간하면 짐벌 락·축 간섭으로 경로가 일그러짐. 작은 각도는 Lerp + 정규화로 근사 가능.

## RotateTowards — 회전 속도 제한

`Quaternion.RotateTowards(현재, 목표, maxDegrees·Time.deltaTime)`로 초당 maxDegrees 이하로만 회전(넘으면 클램프). deltaTime을 곱해 "초당" 일정 각도를 보장(프레임률 무관). Slerp가 "비율 t 보간"이면 RotateTowards는 "초당 고정 각도"라 속도 제한에 맞음.

## 재정규화 — 부동소수점 오차

부동소수점은 실수를 정확히 저장 못 해 회전 행렬/벡터를 오래 곱하면 크기가 1에서 미세하게 벗어남. 회전은 크기를 안 바꿔야 하는데 1.001/0.998로 흐르면 오브젝트가 커지거나 작아지고 계산(내적 = cos)이 틀어짐 → 주기적 재정규화로 단위 길이 유지.

## Lerp 감속 — 체력바/카메라

`Lerp(current, target, t)`를 매 프레임 current를 갱신하며 반복하면, 매번 남은 거리의 t 비율만큼 줄어 목표에 가까울수록 조금씩 줄어듦 → 감속(ease-out). "보간 함수라서"가 아니라 "남은 거리에 비례해 줄어드는 반복"이 이유. 목표에 정확히 안 닿아 보통 스냅 처리.

## MoveTowards vs Lerp

`MoveTowards(a, b, maxDelta)`는 일정 속도로 목표에 정확히 도달(클램프). Lerp는 쓰는 방식에 따라 달라짐 — `Lerp(start, target, t)`로 t를 0→1 선형으로 올리면 일정 속도·정확 도달이지만, `Lerp(current, target, speed·dt)`는 감속(ease-out)이라 영영 정확히 안 닿음.

## 스무스 팔로우 — 프레임률 의존

`Lerp(현재, 목표, t)`를 매 프레임 반복하면 프레임 횟수만큼 비율을 줄이는 지수 감쇠라, 고프레임이 더 빨리 붙음. deltaTime을 곱하는 건 개선이나 지수 특성 때문에 근사일 뿐 완벽하지 않음. 정확한 프레임률 독립은 `1 − exp(−k·dt)` 사용.

## 베지어 곡선

제어점으로 곡선 모양 조종. 3차 베지어는 P0(시작)·P3(끝)·P1·P2(끌어당기는 제어점). 곡선은 제어점 쪽으로 끌리나 닿진 않음. de Casteljau는 Lerp를 중첩하면 곡선이 나오는 방식. 직선 보간과 달리 곡률·자연스러운 가감속·연결(스플라인) 가능.

## 좌표계 — 왼손 vs 오른손

축 방향과 회전의 양(+) 방향을 정하는 규약. 오른손 좌표계는 엄지=x·검지=y·중지=z(OpenGL·수학 관례), 왼손은 z가 반대로 화면 안쪽(유니티·DirectX). 유니티는 왼손·Y-up이라 +z가 전방. 외적의 부호와 회전 방향이 손잡이(handedness)에 따라 뒤집히므로, 다른 엔진·포맷과 데이터를 주고받을 땐 축 변환이 필요.

## 행렬 곱 순서 — TRS

행렬 곱은 교환법칙이 성립하지 않아 순서가 결과를 바꿈. 제자리 회전은 회전 후 이동(T·R), 원점 중심 공전은 이동 후 회전(R·T). 문제에 위치 전제가 없으면 "제자리 회전 = 회전만"도 유효.

## TransformPoint / InverseTransformPoint

로컬↔월드 변환. `TransformPoint`(로컬 → 월드)는 캐릭터 기준 총구 위치를 세계 좌표로(스폰·이펙트). `InverseTransformPoint`(월드 → 로컬)는 월드의 적이 내 기준 앞/뒤인지 판단. 내부는 모델 행렬(TRS)/역행렬 곱이라 부모 회전·스케일이 자동 반영. 방향 벡터엔 `TransformDirection`(이동 무시).

## AABB 충돌

모든 축에서 겹쳐야 충돌. 한 축이라도 분리되면(예: `A.maxX < B.minX`) 그 축에 틈이 있어 충돌이 아님(분리축 정리).

<svg viewBox="0 0 320 175" width="320" style="max-width:100%;height:auto" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="AABB 분리축: x축 투영에 틈이 있어 충돌 아님"><rect x="45" y="35" width="70" height="55" rx="4" fill="#4f83e0" fill-opacity="0.12" stroke="#4f83e0" stroke-width="2"/><text x="80" y="67" font-size="13" fill="#4f83e0" text-anchor="middle">A</text><rect x="165" y="50" width="75" height="55" rx="4" fill="#e2574c" fill-opacity="0.12" stroke="#e2574c" stroke-width="2"/><text x="202" y="82" font-size="13" fill="#e2574c" text-anchor="middle">B</text><line x1="30" y1="140" x2="300" y2="140" stroke="currentColor" stroke-width="1.5" opacity="0.4"/><line x1="45" y1="90" x2="45" y2="140" stroke="#4f83e0" stroke-dasharray="3 3" stroke-width="1" opacity="0.6"/><line x1="115" y1="90" x2="115" y2="140" stroke="#4f83e0" stroke-dasharray="3 3" stroke-width="1" opacity="0.6"/><line x1="165" y1="105" x2="165" y2="140" stroke="#e2574c" stroke-dasharray="3 3" stroke-width="1" opacity="0.6"/><line x1="240" y1="105" x2="240" y2="140" stroke="#e2574c" stroke-dasharray="3 3" stroke-width="1" opacity="0.6"/><line x1="45" y1="140" x2="115" y2="140" stroke="#4f83e0" stroke-width="5"/><line x1="165" y1="140" x2="240" y2="140" stroke="#e2574c" stroke-width="5"/><text x="140" y="135" font-size="12" fill="currentColor" text-anchor="middle" opacity="0.7">틈</text><text x="30" y="165" font-size="12" fill="currentColor" opacity="0.7">x축에 틈 → 이 축이 분리축 → 충돌 아님</text></svg>

## 분리축 정리(SAT)

두 볼록 도형은 어떤 축에 투영했을 때 겹치지 않는 축이 하나라도 있으면 충돌이 아님. AABB는 검사 축이 좌표축뿐이라 간단하고, 회전한 사각형(OBB)·볼록 다각형은 각 변의 법선을 후보 축으로 모두 검사. 하나라도 틈이 있으면 즉시 비충돌로 종료(조기 탈출). 오목 도형엔 직접 못 쓰고 볼록 분해가 필요.

## 원형 충돌과 제곱 거리

두 원은 중심 거리 < r1 + r2이면 충돌. 거리 계산엔 비싼 제곱근이 필요하므로, 양변을 제곱해 거리² < (r1+r2)²로 비교하면 sqrt 없이 같은 판정. 유니티는 `(c1-c2).sqrMagnitude < (r1+r2)²`.

## 광선-구 충돌 판별식

광선 `P(t) = O + t·D`를 구 방정식에 대입 → `at² + bt + c = 0`. 판별식 `D = b² − 4ac`가 음수면 빗나감, 0이면 접함(1점), 양수면 관통(2점). t는 거리 파라미터라 충돌점 거리도 나옴. 두 근이 음수면 광선 뒤쪽이라 실제 충돌이 아님(`t ≥ 0`만 유효).

## 충돌 응답 — 반발 계수와 임펄스

충돌을 감지한 뒤 어떻게 튕길지 정하는 단계. 충돌 법선 방향의 상대 속도에 임펄스(순간적 운동량 변화)를 가해 두 물체 속도를 갱신. 반발 계수 e(0~1)가 튐 정도 — 1이면 완전 탄성(운동에너지 보존), 0이면 완전 비탄성(붙어서 같이 이동). 무거운 쪽이 덜 밀리도록 질량 역수에 비례해 임펄스를 배분.

## Atan2 vs Atan

`Atan`은 입력이 비율 dy/dx 하나라 부호 정보가 날아가 정반대 방향을 구분 못 하고(치역이 −90°에서 +90°), dx = 0이면 0 나누기로 터짐. `Atan2`는 dy·dx를 각각 부호까지 받아 4사분면을 모두 구분(치역이 −180°에서 +180°).

## 원형 배치

i번째 위치 = `(r·cos(2πi/N), r·sin(2πi/N))`. 각을 2π/N씩 나눠 같은 간격. 단위원에서 각 θ인 점의 좌표가 정의상 (cos θ, sin θ)이고(= 단위 벡터를 각 축에 투영한 값), r을 곱하면 반지름 r 원.

## sin 부유 애니메이션

`y = baseY + amplitude × sin(Time.time × frequency)`. sin이 부드러운 이유는 단순히 주기적이어서가 아니라(톱니파도 주기적), 곡선이 연속적이고 꼭대기·바닥에서 속도가 0으로 감속했다 가속하기 때문.

## 포물선 — 등가속도 운동

매 프레임 `velocity += gravity·dt`(수직 속도가 일정량씩 감소 = 등가속), `position += velocity·dt`. 수평은 힘이 없어 등속, 수직은 중력이라는 일정 가속도. 변하는 속도로 위치를 옮겨 궤적이 곡선(포물선). 오일러 적분과 동일.

## 고정 timestep — 물리 적분

가변 프레임률로 물리를 적분하면 dt가 들쭉날쭉해 결과가 프레임률마다 달라지고, 큰 dt에선 터널링(빠른 물체가 벽을 통과)이 생김. 유니티는 물리를 고정 간격 FixedUpdate에서 돌려 재현성을 확보하고, 렌더 프레임과 물리 스텝 사이는 보간으로 메움. 렌더는 Update(가변), 물리는 FixedUpdate(고정)로 분리.

## Time.deltaTime

직전 프레임에서 지금까지 걸린 시간(초). 속도 × deltaTime = 이번 프레임 이동 거리 → 고프레임은 조금씩 자주, 저프레임은 많이 가끔 움직여 1초 총 이동량이 같아짐. 안 곱하면 "프레임당 고정 거리"라 60fps가 30fps의 2배로 움직임. "프레임률의 역수"가 아니라 "한 프레임의 소요 시간 간격"이 정확한 정의.
